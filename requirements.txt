# Django и основные зависимости
Django>=5.2.0,<6.0

# Для работы с документами (импорт из DOCX и PDF)
python-docx>=1.1.0
pdfplumber>=0.10.0

# Jazzmin - современная админ-панель Django
django-jazzmin>=2.6.0

# Pillow для работы с изображениями
Pillow>=10.0.0

# Для обрезки изображений в админке
django-image-cropping>=2.0.0
easy-thumbnails>=2.8.0

# Примечание: Django включает поддержку i18n (локализации) по умолчанию
# Для казахского языка нужно будет создать файлы переводов через:
# django-admin makemessages -l kk -i env
